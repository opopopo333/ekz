<Page x:Class="IKZAMEN1.BookingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IKZAMEN1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="BookingPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="tbMovieTitle" Grid.Row="0" FontSize="18" Margin="10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Дата сеанса:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker x:Name="dpDate" SelectedDateChanged="dpDate_SelectedDateChanged"/>
        </StackPanel>

        <DataGrid x:Name="dgSeats" Grid.Row="2" AutoGenerateColumns="False"
          IsReadOnly="True" SelectionMode="Single" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Место" Binding="{Binding SeatNumber}" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding IsBooked}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Забронировать" Grid.Row="3" Margin="10" Click="BtnBook_Click"/>
    </Grid>
</Page>
